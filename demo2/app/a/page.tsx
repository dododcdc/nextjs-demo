'use client'

import { Button } from 'antd';

export default function a() {

    return (
        <div>
            <Button type="primary" onClick={() => console.log('gg')}>Button</Button>
        </div>
    )
}